# Slidev ページカウント

このプロジェクトはSlidevスライド．ページ番号を数えるときの注意点:

- `slides.md` が各 `pages/*.md` ファイルを `src:` で読み込む
- **1つの `.md` ファイル内に `---` 区切りで複数ページが含まれる場合がある**
- ページ番号を特定するには，`slides.md` の読み込み順に従い，各ファイル内の `---` 区切りも数える必要がある
- ユーザーが「Nページ目」と言った場合，ファイル名ではなく実際のページ順で数えること
